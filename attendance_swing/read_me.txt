simply create a database called attendance_system and import the attendance_system.zip file to it

check the url, username, password inside the DBConnection class and match it to your database details